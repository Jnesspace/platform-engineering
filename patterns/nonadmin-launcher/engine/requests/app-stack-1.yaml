# App-stack request. Add a file like this and open a PR; the onboarding engine
# vends a stack of this name into the team Space. Filename (minus .yaml) = slug.
name: app-stack-1
# project_root: optional override; defaults to the engine's app-example workload.
